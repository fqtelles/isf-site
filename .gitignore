# Dependências
node_modules/

# Next.js build
.next/
out/

# Variáveis de ambiente locais
.env
.env.local
.env.*.local

# Tooling local
.claude/

# Uploads (gerenciados via volume em produção)
public/uploads/*
!public/uploads/.gitkeep

# Banco de dados SQLite (não versionar — produção tem dados reais)
prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm
**/*.db
**/*.db-journal
**/*.db-wal
**/*.db-shm

# OS
.DS_Store
Thumbs.db
